<Window x:Class="NeedleworkStore.Windows.SignDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NeedleworkStore.Windows"
        mc:Ignorable="d"
        Title="Authentication Warning" Height="150" Width="400"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        >
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="Padding" Value="5"></Setter>
            <Setter Property="Width" Value="80"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="10">
            <TextBlock x:Name="MessageTextBlock" Margin="10" TextWrapping="Wrap"
                       Text="Добавить товар в корзину могут только зарегистрированные пользователи."/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" IsCancel="True" Click="Cancel_Click"/>
                <Button Content="Регистрация" Click="Register_Click"/>
                <Button Content="Вход" IsDefault="True" Click="Login_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
